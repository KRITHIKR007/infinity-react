# Initialize Git (if not already done)
git init

# Add all files to Git
git add .

# Commit your changes
git commit -m "Initial commit for Infinity 2025 website"

# Add your GitHub repository as remote origin
git remote add origin https://github.com/your-username/infinity-react.git

# Push to GitHub
git push -u origin main
